// API Configuration
const API_URL = window.location.hostname === 'localhost' 
    ? 'http://localhost:3000'
    : 'orbit30backend-production.up.railway.app';

console.log('🔧 API URL:', API_URL);

